const initialState = {
    score: 0,
    currentLevel: 0,
    isTouched: false
}

export const controlReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}